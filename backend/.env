NODE_ENV=development
PORT=5000

# Render Database Connection Configuration
# 1. Go to your Render Dashboard -> PostgreSQL
# 2. Click "Info" (Connect)
# 3. Copy the "External Database URL"
# 4. Paste it below:
DATABASE_URL=postgresql://gearguard_user:QKB9c6PL2fhyi5G4eSGKZXCE8MXcKHzq@dpg-d57qfgm3jp1c73b4j2e0-a.oregon-postgres.render.com/gearguard

# Fallback Local Config (Ignore if using Render)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gearguard_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=dev_secret_key
JWT_REFRESH_SECRET=dev_refresh_secret
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Cron Jobs
ENABLE_CRON_JOBS=true
